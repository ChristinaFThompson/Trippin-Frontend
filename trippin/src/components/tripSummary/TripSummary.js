import React, { Component } from "react";

class TripSummary extends Component {
  render() {
    return (
      <div>
        <h1>where data is pushed each time resturant is checked</h1>
      </div>
    );
  }
}

export default TripSummary;

// counter function that adds list items from Activity to an array in the form on an unordered list
//  should include date, activity, location and text box
// **bonus
// onclick to email address
